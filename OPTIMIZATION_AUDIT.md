# RapidRAW 软件全面优化审计报告

> 审计日期：2026-08-02  
> 审计对象：当前本地工作区（分支 `codex/merge-upstream-main`，包含 18 个已修改文件与 2 个未跟踪的 pnpm 文件）  
> 审计方式：仓库结构、前后端源码、Tauri 权限、依赖、CI/CD、构建产物、静态检查与测试能力检查  
> 说明：本报告没有修改业务代码。由于工作区包含未提交开发内容，部分校验失败可能来自这些在途改动；正式修复前应在干净的目标分支上复跑基线。

## 1. 结论摘要

RapidRAW 的功能完整度和技术覆盖面已经较高：React 前端、Rust 图像引擎、WGPU、RAW/EXIF、AI 模型、批处理、跨平台打包都已具备。但当前最需要的不是继续叠加功能，而是先补齐“可稳定发布”的工程底座。

当前状态可以概括为：

- **功能能力强，但交付门禁失真**：生产构建能成功，却同时存在 114 个 TypeScript 错误；CI 又把前端格式、Lint、国际化失败设置为可忽略。
- **回归风险很高**：前后端合计约 7.5 万行核心代码，没有发现自动化测试；`cargo test --lib` 实际执行 0 个测试。
- **数据与安全边界需收紧**：CSP 被关闭、主窗口拥有默认 Shell 权限、生产源码硬编码 Clerk 测试环境 key，多项文件命令直接接受任意路径。
- **维护成本正在快速上升**：前端多个单文件超过 2,000 行，Rust 最大文件超过 3,800 行；前端有 205 处 `useEffect`，Rust 有 101 个 Tauri 命令和 204 处 `unwrap`/`expect`/`panic` 类调用。
- **性能仍有明显空间**：前端主 JS 单包约 2.53 MB（gzip 约 748 KB），没有按页面/重功能拆包；AI、编辑器、遮罩等重模块会增加启动和解析成本。

建议先用 2～4 周完成 P0“稳定性冲刺”，再进入性能和体验优化。否则新功能越多，回归定位和跨平台发布成本越高。

## 2. 本次实际检查结果

| 检查项           | 结果               | 关键证据                                                          |
| ---------------- | ------------------ | ----------------------------------------------------------------- |
| 前端生产构建     | 通过，但有体积警告 | 主 JS 2,531.08 KB，gzip 747.97 KB；Vite 提示 chunk 超过 500 KB    |
| TypeScript       | **失败**           | 114 个错误，分布于 36 个文件                                      |
| ESLint           | **失败**           | 879 个问题：830 errors、49 warnings                               |
| Prettier         | **失败**           | 23 个文件格式不一致                                               |
| 国际化同步       | **失败**           | 检出约 469 个待补齐条目/复数变体                                  |
| Rust 格式        | **失败**           | `cargo fmt --all -- --check` 检出差异                             |
| Rust 测试        | 命令通过但无覆盖   | 0 passed、0 failed、0 ignored，实际 0 个测试                      |
| Rust Clippy      | **失败**           | 按 `-D warnings` 验证到 21 个编译/规则错误                        |
| 前端依赖审计     | **失败**           | `npm audit --omit=dev` 报告 3 个 high，链路为 Clerk → `js-cookie` |
| Git 差异基础检查 | 通过               | `git diff --check` 未发现空白符错误                               |

## 3. 优先级总览

### P0：发布前必须处理

| 编号 | 优化项                          | 主要风险                                     | 建议负责人  | 预估        |
| ---- | ------------------------------- | -------------------------------------------- | ----------- | ----------- |
| P0-1 | 修复并强制质量门禁              | 构建“假绿”、运行时错误进入发布               | 前端/DevOps | 3～6 天     |
| P0-2 | 建立最小自动化测试网            | 图像结果、文件操作、批量任务无回归保障       | 全栈        | 1～2 周起步 |
| P0-3 | 收紧 Tauri 安全与权限边界       | Renderer 被利用后可扩大到本地系统            | Rust/安全   | 3～5 天     |
| P0-4 | 移除硬编码测试认证配置          | 正式包连接测试租户、认证/计费异常            | 前端/后端   | 0.5～1 天   |
| P0-5 | 修复文件删除的永久删除回退      | 用户照片不可恢复地丢失                       | Rust/产品   | 1～2 天     |
| P0-6 | 处理高危前端依赖告警            | Cookie 属性注入风险                          | 前端        | 1～2 天     |
| P0-7 | 修复 CI/CD 配置错误并统一版本源 | 格式任务失效、Windows 参数丢失、包版本不一致 | DevOps      | 1～2 天     |

### P1：下一迭代重点

- 拆分前后端巨型模块，建立清晰的领域边界和强类型 IPC。
- 首屏拆包、懒加载 AI/拼图/全景/波形等重模块，建立性能预算。
- 建立图像质量基准、内存/显存预算和跨 GPU 降级策略。
- 补齐国际化、键盘操作、焦点管理和无障碍语义。
- 统一 npm/pnpm、Node/Rust 版本和可复现构建策略。
- 完善任务取消、错误恢复、日志脱敏和故障诊断导出。

### P2：中期建设

- 系统化的性能遥测与长任务分析。
- 插件/算法能力分层，降低核心包耦合。
- 产品内的新手引导、兼容性自检和颜色管理说明。
- 发布签名、SBOM、依赖许可证和供应链审计自动化。

## 4. P0 详细整改项

### P0-1 修复并强制质量门禁

**现状**

- `npm run build` 成功，但 Vite 只转译 TypeScript，不代表类型正确。
- `npm run typecheck` 有 114 个错误；热点包括导航、遮罩、编辑器、图库项、图像处理 Hook。
- ESLint 有 830 个错误，`any`、不安全函数类型、未使用变量和国际化字面量较多。
- [`.github/workflows/lint.yml`](.github/workflows/lint.yml) 对格式、ESLint、i18n 三个步骤都设置了 `continue-on-error: true`，等于不阻止合并。
- CI 没有执行 `npm run typecheck`。

**建议**

1. 先记录当前基线，再分模块清零，不要长期通过扩大 ignore 或降低规则绕过。
2. PR 必须阻断：`typecheck`、`lint`、`format:check`、`i18n:check`、`cargo fmt`、`cargo clippy`、核心测试。
3. 如果一次无法清零，可临时采用“错误数不得增加”的基线脚本，但设置明确移除日期。
4. 把前端生产构建放在类型检查之后，并在本地提供统一命令，例如 `check`。

**验收标准**

- 所有门禁命令 exit code 为 0。
- PR 中任何新增类型、Lint、格式或翻译错误都会失败。
- 分支保护要求这些检查全部通过。

### P0-2 建立最小自动化测试网

**现状**

- 未发现前端 `*.test.*` / `*.spec.*` 或 Rust `tests` 测试文件。
- `cargo test --lib` 编译成功，但执行 0 个测试。
- 应用涉及非破坏编辑、文件移动/删除、EXIF/XMP/`.rrdata`、颜色和 RAW 算法，均属于高回归风险区域。

**建议测试金字塔**

1. **Rust 单元测试**：调整参数解析、色彩转换、裁剪坐标、文件名模板、XMP/sidecar 迁移、镜头匹配。
2. **图像金样测试**：固定输入图 + 固定参数，比较输出 hash、感知差异或像素容差；按 CPU/WGPU 能力分层。
3. **文件事务测试**：全部在临时目录验证复制、移动、重命名、冲突、回收站失败、磁盘满和权限不足。
4. **前端组件/Store 测试**：撤销重做、批量选择、筛选排序、导出任务状态、事件清理。
5. **Tauri 契约测试**：命令参数、返回值、事件 payload 由共享 schema 生成或校验。
6. **跨平台冒烟测试**：启动、导入 JPEG/RAW、编辑、导出、重新打开 sidecar；至少覆盖 Windows/macOS/Linux。

**第一阶段覆盖目标**

- 不追求虚高的全局覆盖率，先覆盖“会丢数据”和“会改变图像结果”的路径。
- 文件操作、sidecar、导出参数、撤销重做的分支覆盖率 ≥ 80%。
- 每个已支持主要图像格式至少有 1 个合法样本和 1 个损坏样本。

### P0-3 收紧 Tauri 安全与权限边界

**现状**

- [`src-tauri/tauri.conf.json`](src-tauri/tauri.conf.json) 中 `csp` 为 `null`。
- [`src-tauri/capabilities/default.json`](src-tauri/capabilities/default.json) 向主窗口授予 `shell:default` 和 `process:default`。
- 后端暴露 101 个 Tauri 命令，多个文件命令直接接收前端传入的字符串路径。
- 应用会加载远程内容/接口，包括 GitHub、OpenStreetMap、Clerk、Hugging Face 和 RapidRAW 云服务；Renderer 一旦出现脚本注入，权限组合会扩大影响。

**建议**

1. 配置明确 CSP；把 `connect-src`、`img-src`、`frame-src` 精确限制到实际域名，避免 `*`。
2. 移除未使用的 Shell/Process 默认权限；必须使用时改为命令级 allowlist 和参数约束。
3. 对高风险文件命令增加统一路径策略：规范化路径、拒绝非法遍历、校验目标类型、明确覆盖策略。
4. 对删除、移动、覆盖等操作使用后端签发的短期操作令牌，避免任意 IPC 调用直接执行。
5. OpenStreetMap iframe、远程预设和外部链接隔离处理，验证 URL scheme，禁止 `file:`/自定义危险 scheme。
6. 对所有下载设置超时、大小上限、原子落盘和 hash 校验。当前 AI 模型大多已有 SHA-256，但 `clip_tokenizer.json` 的 hash 仍为 `None`，应补齐。

**验收标准**

- Tauri 安全配置通过专门审查；主窗口没有无业务需要的通配权限。
- 所有外部域名均有清单、用途、数据类型和隐私说明。
- 文件命令有越权路径、符号链接、覆盖和竞态测试。

### P0-4 移除硬编码测试认证配置

**现状**

- [`src/App.tsx`](src/App.tsx) 直接硬编码 `pk_test_...` Clerk publishable key，并注释为 `local dev key`。
- 该值会被打进生产 JS，正式包可能连接测试租户；publishable key 不是服务端 secret，但环境串用仍会造成登录、计费、用户隔离和可用性问题。

**建议**

- 改为构建环境变量，并分别维护 dev/staging/prod 配置。
- 构建时校验：Release 环境禁止 `pk_test_`，缺失配置直接失败。
- 云 AI 的 API base URL 同样环境化并做 allowlist。

**验收标准**

- 仓库无硬编码测试租户 key。
- Release 构建产物自动检查不包含 `pk_test_`、localhost 或 staging 域名。

### P0-5 禁止“回收站失败后静默永久删除”

**现状**

- [`src-tauri/src/file_management.rs`](src-tauri/src/file_management.rs) 中，删除文件夹或移动源文件到回收站失败后，会回退到 `remove_dir_all` / `remove_file` 永久删除。
- 这与用户选择“移到回收站”的恢复预期不一致，照片和 sidecar 可能不可恢复。
- 移动实现采用“先复制、后删除”，缺少完整事务日志时，部分失败可能留下重复或半完成状态。

**建议**

- 回收站失败时停止并提示，永久删除必须二次确认且明确不可恢复。
- 批量移动采用事务清单：预检空间/权限/冲突 → 复制到临时名 → 校验大小/hash → 原子改名 → 删除源 → 写完成记录。
- 中断后提供恢复/清理机制，不要静默吞掉 sidecar 复制错误。

**验收标准**

- 未经明确永久删除确认，不调用永久删除 API。
- 注入权限不足、磁盘满、文件占用、应用崩溃后，源文件仍可恢复且状态可解释。

### P0-6 处理依赖安全告警

**现状**

- `npm audit --omit=dev` 报告 3 个 high，根因链路是 `@clerk/react` → `@clerk/shared` → `js-cookie <= 3.0.5`，对应 cookie 属性注入公告。
- 本地依赖目录呈 pnpm 链接结构，但 CI 仍使用 npm；审计结果需要在包管理器统一后再复核。

**建议**

- 先确认 Clerk 官方修复版本或 dependency override 是否安全可用；没有修复版本时评估替换/隔离 cookie 使用路径。
- CI 增加生产依赖审计和 Rust 依赖审计，按严重级别阻断，并允许有到期日的书面豁免。
- 生成 SBOM，发布时归档依赖与许可证清单。

**验收标准**

- high/critical 为 0，或存在带负责人、影响分析、补偿措施和到期日的豁免。

### P0-7 修复 CI/CD 与版本一致性

**发现**

1. [`.github/workflows/ci.yml`](.github/workflows/ci.yml) 的 Windows 矩阵字段写成 `builds-args`，传递时读取的是 `matrix.args`，因此 `--bundles nsis` 实际丢失；PR 和 release workflow 也有同样模式。
2. [`.github/workflows/lint.yml`](.github/workflows/lint.yml) 执行 `cargo fmt -p RapidRAW`，而当前 Cargo package 名为 `MonoRin`，本地 metadata 版本还是 `0.0.0`。
3. Tauri 版本为 `1.5.7`，Cargo metadata 为 `0.0.0`，npm package 又未声明产品版本。
4. build workflow 使用 `npm install` 而非冻结锁文件安装；仓库当前同时存在 npm 与 pnpm 锁文件。
5. Lint 使用 Node 20，Build 使用 Node 22，开发环境未由 `.nvmrc`/Volta 等固定。

**建议**

- 选择 npm 或 pnpm 作为唯一包管理器，删除另一套锁文件，CI 使用 `npm ci` 或 `pnpm install --frozen-lockfile`。
- 建立单一版本源，脚本同步 Tauri、Cargo、安装包和元数据版本，并在 CI 验证。
- 修复矩阵字段名和 Cargo package 名；给 reusable workflow 加输入校验。
- 发布前增加“只构建不发布”的全平台候选验证；正式发布使用同一 commit 和同一锁文件。

## 5. 架构与可维护性优化

### 5.1 拆分巨型前端组件

主要热点：

| 文件                | 约行数 | 建议边界                                                     |
| ------------------- | -----: | ------------------------------------------------------------ |
| `SettingsPanel.tsx` |  2,640 | 按 General/Processing/AI/Data/About 分 tab + schema 驱动表单 |
| `ImageCanvas.tsx`   |  2,600 | 渲染器、输入状态机、裁剪、遮罩绘制、坐标转换分层             |
| `MasksPanel.tsx`    |  2,560 | Mask tree、属性编辑器、组合操作、DnD 分离                    |
| `Editor.tsx`        |  2,051 | 会话控制器、历史、渲染调度、布局展示分离                     |
| `AIPanel.tsx`       |  2,011 | provider、任务流、模型管理、视图组件分离                     |

建议单个展示组件尽量控制在 300～500 行，复杂业务通过领域 Hook/Store/service 分层。不要只做机械切文件，应同时减少跨层状态和重复 effect。

### 5.2 拆分 Rust 巨型模块

| 文件                  | 约行数 | 建议边界                                                   |
| --------------------- | -----: | ---------------------------------------------------------- |
| `image_processing.rs` |  3,854 | tone/color/detail/geometry/pipeline/command adapter        |
| `file_management.rs`  |  3,621 | catalog、sidecar、filesystem transaction、thumbnail、album |
| `lib.rs`              |  2,360 | bootstrap、commands、window lifecycle、logging、workers    |
| `gpu_processing.rs`   |  2,019 | context、surface、cache、render graph、fallback            |
| `ai_processing.rs`    |  1,849 | model registry、download、session、mask/depth/tag task     |

为每个领域定义统一错误类型，命令层只负责参数验证和 DTO 转换。核心算法不依赖 Tauri，才能直接单测和复用。

### 5.3 建立强类型 IPC 与事件协议

- 101 个 Tauri 命令、多个字符串事件和大量 `event: any` 已形成“隐式 API”。
- 建议从 Rust DTO 生成 TypeScript 类型，或使用共享 schema；命令名、事件名集中注册。
- 所有长任务统一采用 `{ taskId, phase, current, total, message, errorCode }` 协议，支持取消、重试和幂等。
- 前端监听器使用统一 registry，确保组件卸载、StrictMode 重挂载和窗口重建时不会重复订阅。

### 5.4 降低 panic 和锁中毒风险

- Rust 核心代码中约有 204 处 `unwrap`/`expect`/`panic` 类调用，另有 20 处 `unsafe` 关键词命中。
- 图像缓冲区尺寸不变量、正则常量等可以保留受证明的 expect，但必须写明不变量；文件路径、锁、网络、用户输入不应 panic。
- 为 GPU/AI/文件任务定义可展示错误码，避免只向前端返回不可本地化的字符串。
- 审查锁持有期间是否执行 I/O/推理；长任务使用专用 worker、信号量和取消令牌。

## 6. 性能与资源优化

### 6.1 前端启动性能

**问题**：当前生产构建几乎集中为一个 2.53 MB JS 包。

**建议顺序**

1. 对 AI 面板、设置、全景、HDR、拼图、负片、波形、Konva 编辑器使用动态 import。
2. 首屏只加载库浏览和基础框架；进入编辑器后再加载裁剪/遮罩/动画依赖。
3. 分析 `framer-motion`、Konva、图标库、颜色组件的实际导入，避免整库进入主包。
4. 建立预算：首屏 gzip JS 建议先降到 300～400 KB，单异步 chunk 控制在 300～500 KB。
5. 记录冷启动、可交互时间和打开首张 RAW 的 P50/P95，而不仅看打包体积。

### 6.2 图像管线与 GPU

- 为“导入 → 解码 → 预览 → 首帧 → 高精度替换”分阶段打点。
- 建立 24MP、45MP、100MP 和超大全景基准集，记录耗时、峰值 RAM、VRAM、缓存命中率。
- 预览调整采用最新任务获胜和可取消机制；避免旧任务完成后覆盖新参数。
- 对老 GPU、集显、软件渲染分别配置质量档，启动时做短基准而不是只按设备名判断。
- 对 shader pipeline、LUT、mask、geometry cache 设明确容量和淘汰指标；内存压力时主动降级。
- 为 GPU 丢失、显存不足和驱动崩溃提供 CPU/低质量回退及用户可理解的诊断。

### 6.3 AI 模型与安装体积

- 本地工作区模型目录约 1.3 GB；由于其被 `.gitignore` 排除，干净 CI 包未必包含这些模型，但 `bundle.resources = ["resources", ...]` 会让本地打包结果受目录内容影响。
- 建议资源清单显式列文件，不要将整个可变目录打包；CI 输出每个安装包的资源清单和大小。
- 模型按功能按需下载，展示大小、许可证、hash、下载进度、暂停/重试和清理入口。
- `clip_tokenizer.json` 也应校验 SHA-256；所有下载先写临时文件、验证后原子替换。
- 明确“基础安装大小”和“全部本地 AI 模型占用”，避免文档中的轻量描述与用户实际磁盘占用产生落差。

### 6.4 文件库与缩略图

- 大图库扫描改为增量索引，持久化目录 mtime/file id/hash，避免每次全量遍历。
- 缩略图队列应有可见区域优先级、去重、取消和 backpressure。
- 删除/移动/重命名后使用事件增量更新 Store，避免全树重扫。
- 对网络盘、外接盘、符号链接、大小写不敏感文件系统和路径失效建立专项测试。

## 7. 用户体验与产品优化

### 7.1 长任务体验

- 导入、导出、AI、降噪、HDR、全景、culling 已有多类事件，但应统一到任务中心。
- 每个任务显示阶段、进度、预计剩余、目标路径、可取消/重试；应用重启后能解释未完成任务。
- 批量导出应提供失败清单，而不是“部分成功”只显示一个总状态。

### 7.2 数据安全与可恢复性

- 增加 sidecar 自动备份和 schema version；升级前迁移，失败时保留原文件。
- 提供“校验图库”“修复丢失引用”“重新关联移动文件”。
- 退出、切图或覆盖设置前明确提示仍在保存/导出的任务。
- 为预设、设置、快捷键和图库元数据提供一键备份/恢复。

### 7.3 无障碍和键盘操作

- 当前存在不少自定义 dropdown、slider、canvas 和可点击 `div`，需要完整键盘与读屏测试。
- Dialog 应统一焦点陷阱、初始焦点、Escape、关闭后焦点恢复；Tab/菜单使用标准 roving tabindex。
- Canvas 工具提供文本状态和可键盘调整方式；颜色不能作为评级/状态的唯一表达。
- 自动化检查建议加入 axe，并手测 200% 缩放、高对比度、减少动画和屏幕阅读器。

### 7.4 国际化

- 本次 i18n 检查约有 469 个待同步条目/复数变体，俄语等语言的 plural forms 尤其明显。
- 英文作为 source locale，其他语言缺失时应明确回退，不展示空字符串。
- CI 阻止缺 key、空翻译和动态 key 无类型校验；重要流程做伪本地化，检查文本膨胀。

### 7.5 专业摄影工作流

建议在稳定性完成后评估：

- 显示器 ICC、导入色彩空间、工作色域、软打样和导出 profile 的完整闭环。
- 导出锐化（屏幕/哑光/光面）、像素密度和社交平台预设。
- Before/After、参考图、同步编辑的更明确状态，避免用户不知道哪些图已写入 sidecar。
- 首次启动兼容性自检：GPU backend、模型磁盘空间、色彩管理、RAW 支持和日志位置。

## 8. 功能层全面优化与新增建议

本章专门从产品功能出发，不重复前面的代码质量和性能整改。当前项目已有较完整的基础功能，后续重点应是打通工作流、降低误操作风险、提升专业输出一致性，而不是单纯增加更多滑杆。

### 8.1 当前功能覆盖与主要缺口

| 工作流 | 当前已有能力                                        | 主要需要优化                                         | 建议补充能力                                |
| ------ | --------------------------------------------------- | ---------------------------------------------------- | ------------------------------------------- |
| 导入   | 文件导入、保留目录、进度事件                        | 缺少完整导入预检、校验和失败恢复                     | 双份备份、校验复制、导入命名与目录模板      |
| 图库   | 文件夹、递归浏览、相册、标签、评级、颜色、智能搜索  | 相册与搜索结果缺少持久化规则和大图库数据库能力       | 智能相册、堆叠、时间线、地图、离线卷重连    |
| 选片   | 相似图分组、模糊检测、质量评分                      | 审核效率、解释性、人物闭眼/表情判断不足              | 对比选片台、连拍堆叠、人物/眼睛质量辅助     |
| 编辑   | RAW、曲线、HSL、调色、细节、效果、镜头、变形        | 专业色彩管理、局部工具一致性和批量同步需要加强       | ICC/软打样、快照、多版本对比、修复/克隆增强 |
| 遮罩   | 画笔、组合遮罩、AI 主体/天空/前景/深度              | 边缘修正、遮罩诊断、跨图同步可控性不足               | 发丝细化、颜色/亮度范围、对象跟踪式复制     |
| AI     | 本地模型、自托管 Connector、云服务接口              | 模型/隐私/成本状态不够统一，结果缺少可解释和可恢复性 | AI 任务中心、用量预估、模型版本和结果溯源   |
| 元数据 | EXIF 查看/部分编辑、GPS 展示、标签、XMP             | 批量 IPTC、层级关键词和时区校正不足                  | 元数据模板、地图编辑、隐私清理预览          |
| 导出   | JPEG/PNG/TIFF/WebP/JXL/AVIF、尺寸、命名、水印、预设 | 色彩空间、位深、锐化、冲突策略和失败恢复不足         | ICC/profile、目标体积、输出锐化、发布队列   |

### 8.2 现有功能需要优先优化

#### A. 导入与素材落盘

**需要优化**

- 导入前展示源文件数量、总大小、目标剩余空间、重复文件和不支持格式。
- 区分“复制成功”“校验成功”“sidecar 成功”“缩略图已生成”，不要用单一完成状态掩盖部分失败。
- 导入中断后可继续，不应重新复制全部素材。
- 对相机卡、网络盘、移动硬盘显示连接状态，并在安全弹出前确认所有任务完成。
- 重名策略让用户预览：跳过、覆盖、生成新名称、按内容 hash 去重。

**建议补充**

- 导入时同时写入主库和第二备份位置。
- 文件复制后执行 size + hash 校验，可选快速校验/完整校验。
- 目录模板：`{YYYY}/{YYYY-MM-DD}_{shoot}`；文件名支持相机、日期、序号、原文件名等变量。
- 自动应用元数据模板、版权信息、关键词、开发预设。
- 保存导入会话，支持查看“本次导入”“上次导入失败项”。

**验收标准**

- 拔掉移动硬盘、磁盘空间不足或导入中断时，界面能准确列出成功、失败、未开始文件。
- 启用完整校验后，只有 hash 一致的文件才标记为可安全删除源卡内容。

#### B. 图库、相册与搜索

**需要优化**

- 当前搜索支持文本、标签和相机参数表达式，应增加可发现的查询构建器，用户不必记语法。
- 搜索、筛选和排序组合应可以保存、命名、置顶和共享。
- 相册中的丢失文件、移动文件和虚拟副本需要明确状态，提供批量重新关联。
- 大图库不能每次靠目录扫描和内存过滤，应有持久化索引和增量更新。
- 相册/标签操作增加撤销，批量改错后能够恢复。

**建议补充**

- **智能相册**：用规则动态维护，例如“最近 30 天、评分 ≥ 4、Sony、未导出”。
- **照片堆叠**：按拍摄时间、文件名序列或相似度自动堆叠连拍/HDR/全景源片。
- **时间线视图**：年/月/日/拍摄事件浏览，支持跨文件夹选择。
- **地图视图**：按 GPS 聚合，支持拖动校正位置、批量移除位置。
- **离线卷支持**：记住外接盘卷标/文件 ID，卷不在线时仍能搜索预览，重新连接后自动恢复。
- **重复照片管理**：区分完全重复、视觉近似、RAW+JPEG 伴生和不同导出版本。
- **层级关键词**：如“地点 > 中国 > 上海”，支持同义词和关键词模板。

**验收标准**

- 10 万张图库首次索引后，二次启动无需全量重扫；普通过滤/搜索在目标硬件上 P95 < 300 ms。
- 智能相册规则可以导出/导入，并在文件元数据变化后自动刷新。

#### C. 选片与 Culling

**需要优化**

- 当前相似图/模糊图建议应显示评分依据和置信度，避免让用户误以为 AI 判断必然正确。
- 提供 2～6 张同步对比，统一缩放、拖动和焦点点位查看，而不是只看小缩略图。
- 明确“保留、待定、拒绝”三态；应用建议前提供可撤销预览。
- 模糊判定应区分主体运动、相机抖动、景深外背景和有意柔焦。
- 对筛选结果提供快捷键连续操作，减少鼠标往返。

**建议补充**

- 连拍自动分组，并推荐组内最佳照片。
- 100% 放大同步查看眼睛/人脸/焦点区域，提供焦点热区叠加。
- 人像辅助指标：闭眼、严重遮挡、表情异常、多人合影中有人闭眼；默认本地处理。
- 曝光异常、纯黑/纯白、严重噪点、重复构图单独分类，不直接混入“删除建议”。
- Culling 会话：记录处理到哪一张，可跨重启继续。

**验收标准**

- 所有自动拒绝动作默认只标记、不删除；用户可一键撤销整次选片操作。
- 选片台可全键盘完成翻页、对比、评分、颜色、保留/拒绝。

#### D. 编辑器与非破坏工作流

**需要优化**

- 将历史记录做成可理解的动作名称，并能快速跳转，不只提供撤销/重做按钮。
- 保存状态明确区分“正在渲染”“已写 sidecar”“尚未保存”“保存失败”。
- Copy/Paste 和批量同步提供差异预览，明确是否包含裁剪、镜头、遮罩、AI patch。
- Before/After 支持左右、上下、分割线、按住临时查看和参考图对比。
- 自动调整和智能色调显示具体改变项，允许单项接受/拒绝。
- 局部遮罩内可用的调整项应尽量与全局一致，并解释性能受限项。

**建议补充**

- **命名快照**：同一虚拟副本内保存多个编辑节点，例如“自然”“黑白”“客户版”。
- **参考图模式**：锁定参考照片，与当前照片同步缩放或独立缩放。
- **相对同步**：批量照片按各自白平衡/曝光基线应用相对增量，而不是覆盖为同一绝对值。
- **参数搜索/命令面板**：快速定位调整项、命令和快捷键。
- **自动水平与透视 Upright**：提供自动、垂直、水平、完全四种策略，并显示裁切损失。
- **修复/克隆工具增强**：自动源点、手动源点、内容识别、纹理/颜色分离，支持批量清除传感器灰尘。
- **黑白混色器**：独立控制各颜色转灰后的亮度，并支持分离色调。
- **颜色范围和亮度范围遮罩**：吸管多点采样、范围可视化、平滑/扩展/收缩。

**验收标准**

- 任意编辑动作崩溃重启后，最近一次已确认操作可恢复且历史不损坏。
- 批量同步前后可展示每张图将变更的参数集合，并支持整批撤销。

#### E. 色彩、RAW 与图像质量

这是专业图像软件最需要补齐的功能层，不应只作为底层算法优化处理。

**需要优化**

- 在界面明确相机输入 profile、工作色域、显示 profile 和导出 profile，避免用户不知道颜色经过了什么转换。
- RAW 高光恢复提供不同策略和预览，避免一个全局参数覆盖所有相机。
- 相机白平衡、自动白平衡、自定义吸管和多点中性区域应有清晰切换。
- 镜头自动识别失败时给出原因：EXIF 缺失、机身/镜头名称不匹配或数据库缺失。
- 对锐化、降噪和纹理调整提供 100% 查看提醒，避免缩略预览误判。

**建议补充**

- ICC/显示器 profile 管理和软打样。
- 导入 DCP/ICC 相机 profile，并支持按机身设置默认 profile。
- 色域外警告、黑白点/高光裁切警告分别可配置。
- 色差、暗角、畸变的手动 profile 保存和社区共享。
- X-Trans 等特殊传感器的质量/速度模式；对不支持路径显示明确降级提示。
- 传感器坏点/热像素映射和长期灰尘修复模板。

**验收标准**

- 标准色卡和参考图在支持的显示环境中具有可重复结果；导出重新导入后的颜色差异在定义容差内。
- 不同 RAW 解码质量模式有文档化的速度、内存和画质对比。

#### F. 遮罩与局部调整

**需要优化**

- 遮罩叠加支持多种颜色/不透明度、黑白查看、边缘查看和临时隐藏。
- AI 遮罩需要“添加/减去画笔”“边缘细化”“羽化前后对比”和失败重试。
- 复杂遮罩树显示实际组合关系，提供禁用节点、独奏节点和局部缓存状态。
- 跨照片复制遮罩时明确哪些是坐标遮罩、哪些可重新识别主体，避免错位。

**建议补充**

- 人物细分：面部皮肤、身体皮肤、头发、牙齿、眼睛、衣服。
- 对象选择：点击物体生成遮罩，并允许框选辅助。
- 自适应预设：把“主体提亮、背景压暗”等语义规则保存为预设，应用到新图时重新计算遮罩。
- 遮罩与修复工具的版本化缓存，模型更新后可选择是否重新生成。

#### G. AI 功能

**需要优化**

- 在执行前明确当前 provider：本地、自托管或云；是否上传原图、上传尺寸、预计用量和费用。
- AI 任务统一排队，支持暂停、取消、重试和后台运行；模型加载不应让用户误以为卡死。
- 对输出保存 prompt、provider、模型版本、seed、处理尺寸和时间，便于复现。
- 云端失败时不要自动切换 provider 或重复计费，必须明确提示。
- AI 生成结果以新 patch/虚拟副本存在，不覆盖原有编辑。

**建议补充**

- 自然语言编辑先生成“参数计划”，让用户预览将修改哪些滑杆/遮罩，再确认执行。
- AI 标签管理：同义词合并、置信度阈值、批量审核、禁止词和人物隐私开关。
- 本地模型能力检测，根据 CPU/GPU/RAM 推荐模型规格和并发数。
- 生成式填充提供边缘融合、色调匹配和高分辨率局部重绘。
- 为 AI 输出写入可选的内容来源/编辑说明，方便后续版权和交付管理。

**验收标准**

- 用户能在任务开始前知道数据是否离开本机，并能在设置中永久禁止云上传。
- 每个 AI 结果都能追溯配置并独立删除，不破坏其他编辑。

#### H. 元数据、版权与隐私

**需要优化**

- 批量编辑标题、描述、作者、版权、联系方式、关键词时显示覆盖/追加/移除模式。
- GPS 不只展示地图，还应支持编辑、复制、批量清除和导出前隐私预览。
- 拍摄时间支持按时区、相机时钟误差批量平移。
- XMP 与 `.rrdata` 同时存在时明确各自数据职责和冲突优先级。

**建议补充**

- IPTC 元数据模板：摄影师、版权、授权、地点和客户信息。
- 批量时间校正，可用一张手机照片作为参照计算偏移。
- 元数据差异查看：源 RAW、XMP、sidecar、当前内存和将导出文件之间的区别。
- 导出隐私检查：GPS、序列号、人物标签、AI prompt 等敏感信息逐项确认。

#### I. 导出与交付

**需要优化**

- 每个格式展示适用能力：位深、透明度、元数据、ICC、HDR、压缩方式，而不是只提供格式名。
- 文件冲突策略必须明确：跳过、覆盖、自动编号、仅较新版本覆盖。
- 导出前估算文件数、总大小、耗时和剩余磁盘空间。
- 失败后提供逐文件原因和“仅重试失败项”。
- 导出预设需要版本和导入冲突处理，避免同名静默覆盖。

**建议补充**

- 输出色彩空间/profile：sRGB、Display P3、Adobe RGB、ProPhoto RGB 和嵌入自定义 ICC。
- TIFF/PNG/JXL/AVIF 的位深、无损/有损、色度抽样和 HDR 元数据选项。
- 输出锐化：屏幕、哑光纸、光面纸，低/标准/高强度。
- 目标文件大小模式，例如“每张 ≤ 5 MB”，自动寻找质量参数。
- DPI、长短边、兆像素、百分比、画布适配和留白边框。
- 联系表/PDF 校样、客户选片编号和包含文件名的水印。
- 导出后动作：打开目录、运行受限脚本、复制到第二位置或调用发布适配器。

**验收标准**

- 同一导出预设在三平台生成一致的命名、尺寸、profile 和元数据策略。
- 导出中止/重启不会覆盖已成功文件，也能仅重试失败项。

#### J. 界面、工作区与输入设备

**需要优化**

- 设置项已经很多，应增加设置搜索、恢复默认、变更标记和“需要重启”提示。
- 快捷键编辑器检测冲突，区分全局、图库、编辑器、遮罩上下文。
- 用户调整面板、波形、工具顺序后可保存为工作区。
- 触控板、鼠标、数位板的缩放/画笔行为独立配置，显示实时压力状态。
- 退出、切换文件、切换目录时统一处理未完成保存和后台任务。

**建议补充**

- 命令面板：搜索功能、跳转设置、执行批处理、查看快捷键。
- 多显示器模式：主屏编辑，副屏全屏预览/参考图；可选使用各自 ICC。
- 新手引导按真实任务组织：导入 → 选片 → 编辑 → 批量同步 → 导出。
- 功能发现机制：首次使用轻提示、可搜索帮助、参数右键重置/复制/加入收藏。
- 可自定义工具收藏和“简洁/专业”界面密度。

### 8.3 推荐新增功能优先级

#### F0：与数据安全直接相关，建议并入 P0

| 新增功能                  | 用户价值 | 复杂度 | 推荐原因                       |
| ------------------------- | -------- | ------ | ------------------------------ |
| 导入校验和第二备份        | 极高     | 中     | 防止素材在导入环节丢失         |
| 文件操作撤销/事务恢复     | 极高     | 高     | 复制、移动、删除是最高风险功能 |
| 导出冲突预检和失败重试    | 高       | 中     | 避免覆盖成片和整批重跑         |
| sidecar schema 版本与恢复 | 极高     | 中     | 保证非破坏编辑长期可读         |
| 云 AI 数据流与费用确认    | 高       | 中     | 隐私、计费和信任基础           |

#### F1：近期最值得做，建议 1～3 个版本内完成

| 新增功能                | 用户价值 | 复杂度 | MVP 范围                              |
| ----------------------- | -------- | ------ | ------------------------------------- |
| 智能相册/保存搜索       | 高       | 中     | 保存现有查询规则并自动刷新            |
| 连拍堆叠 + 多图同步对比 | 高       | 中高   | 时间分组、2～6 图同步缩放、最佳图标记 |
| 命名快照与参考图        | 高       | 中     | 编辑快照、左右/分割对比、锁定参考图   |
| 导出色彩空间/位深/锐化  | 极高     | 高     | 先完成 sRGB/P3、8/16 bit、屏幕锐化    |
| 元数据模板与隐私检查    | 高       | 中     | IPTC 模板、GPS/序列号导出确认         |
| 相对批量同步            | 高       | 中高   | 白平衡/曝光按基线增量同步             |
| 设置/命令搜索           | 中高     | 低中   | 搜索设置、调整项、命令和快捷键        |

#### F2：中期差异化能力

| 新增功能           | 用户价值       | 复杂度 | 前置条件                          |
| ------------------ | -------------- | ------ | --------------------------------- |
| 时间线与地图图库   | 中高           | 高     | 持久化图库索引、GPS 数据模型      |
| 人脸/人物本地聚类  | 高（人像用户） | 高     | 隐私开关、模型管理、人工审核      |
| 自适应预设         | 高             | 高     | 稳定遮罩协议与规则引擎            |
| 多显示器软打样     | 高（专业用户） | 高     | ICC/显示色彩管理                  |
| 传感器灰尘批量修复 | 中高           | 中高   | 稳定修复工具和坐标映射            |
| 外部编辑器往返     | 中高           | 中     | TIFF/PSD 交接、文件监控、版本关系 |

#### F3：长期生态能力，验证需求后再做

- 联机拍摄（Tethering）与自动导入监看。
- 打印布局、纸张 profile、软打样和打印锐化闭环。
- 客户选片 Web Gallery、评论和批准状态同步。
- 发布适配器或受限插件 SDK。
- 团队共享图库、版本评论和权限管理。
- 跨设备设置/预设同步；照片原片同步不建议在核心稳定前自建。

### 8.4 不建议近期优先投入的功能

- **完整 PSD 图层编辑器**：会把产品带入另一条复杂度极高的路线，优先做好外部编辑器往返。
- **自建全量云盘同步**：涉及存储、冲突、隐私、费用和长期运维，应先支持用户自选目录/第三方同步。
- **开放式插件执行任意代码**：当前权限边界尚未收紧，插件系统应在沙箱、API 稳定和签名机制完成后再做。
- **继续增加大量风格特效滑杆**：核心色彩、导入、选片、可靠导出带来的用户价值更高。
- **完全自动删除“坏片”**：只能做建议和标记，不能替用户进行不可恢复删除。

### 8.5 功能路线图建议

```text
F0 数据安全
  导入校验/备份 → 文件事务/撤销 → sidecar 恢复 → 导出冲突保护

F1 工作流闭环
  智能相册 → 连拍堆叠/对比选片 → 快照/参考图 → 专业导出

F2 专业能力
  ICC/软打样 → 元数据模板/隐私 → 相对批量同步 → 自适应预设

F3 差异化生态
  时间线/地图 → 本地人物聚类 → 外部编辑器 → 联机拍摄/客户交付
```

功能优先级应由以下指标驱动：

- 完成一次“导入 → 选片 → 批量编辑 → 导出”的平均时间和失败率。
- 因文件丢失、覆盖、sidecar 损坏导致的高严重度问题数。
- 1 万/10 万张图库的搜索和滚动 P95。
- 选片每分钟处理张数、快捷键使用率和自动建议采纳率。
- 批量导出成功率、重试率、颜色/尺寸配置错误率。
- 新增功能的 30 天使用留存，而不是仅统计入口点击。

## 9. 可观测性与故障诊断

- 当前已有 Rust/前端日志桥和 panic hook，这是良好基础；下一步应结构化并统一 taskId/imageId（对路径做脱敏）。
- 增加“导出诊断包”：版本、OS、GPU/driver、backend、模型 hash、最近日志、功能开关；默认不包含照片和完整文件路径。
- 崩溃上报必须 opt-in，并在 UI 中说明收集字段、保存期限和关闭方式。
- 建立错误码目录，例如 `FS_PERMISSION_DENIED`、`GPU_DEVICE_LOST`、`MODEL_HASH_MISMATCH`，UI 再本地化。
- 性能遥测关注 P50/P95：启动、扫描 1 万张图库、首张预览、滑杆帧延迟、导出吞吐、峰值内存。

## 10. 构建、发布与仓库治理

### 必做

- 锁定 Node、Rust toolchain、包管理器和依赖锁文件。
- CI 使用最小权限；发布 job 才授予 `contents: write`，普通构建只读。
- GitHub Actions 能用 commit SHA 的尽量固定，并定期通过 Dependabot/Renovate 更新。
- 统一 RapidRAW/MonoRin 的产品名、二进制名、Cargo package、应用 identifier 和文档口径；identifier 若涉及用户数据目录迁移，必须保留兼容策略。
- 每个 release 生成 changelog、checksums、SBOM、签名/公证状态和已知问题。

### 建议增加的 CI 阶段

```text
install(frozen)
  → typecheck + lint + format + i18n
  → rustfmt + clippy + unit tests
  → frontend build + bundle budget
  → image golden tests
  → Tauri smoke build
  → platform package/sign/notarize
  → SBOM + checksum + artifact manifest
```

## 11. 分阶段执行路线图

### 第 1 阶段：稳定性冲刺（第 1～2 周）

- 修 CI 配置，加入 typecheck，取消 `continue-on-error`。
- 清零类型、Lint、格式、i18n、Clippy 错误。
- 移除 Clerk 测试 key，收紧 CSP/Shell 权限。
- 移除永久删除回退，补文件事务失败测试。
- 统一 npm/pnpm 和版本源。

**退出标准**：所有 PR 门禁真实为绿；不会在无明确确认时永久删除用户文件。

### 第 2 阶段：回归保护（第 3～5 周）

- Rust 核心算法/sidecar/文件操作测试。
- 前端 Store、任务状态和关键组件测试。
- 建立图像金样与三平台冒烟测试。
- 强类型 IPC 与统一任务协议先覆盖导入/导出/AI。

**退出标准**：高风险核心流程有自动测试；发布候选可一键复验。

### 第 3 阶段：架构与性能（第 6～10 周）

- 拆分五个巨型前端组件和五个 Rust 模块。
- 路由/功能懒加载，主包 gzip 降至 300～400 KB 目标区间。
- 建立 RAW 打开、滑杆延迟、批量导出、内存/显存基准。
- 完成模型资源显式清单与磁盘管理。

**退出标准**：关键性能有可重复数据，回归能在 CI 或基准机上被发现。

### 第 4 阶段：专业体验（持续）

- 色彩管理闭环、无障碍、诊断包、新手兼容性自检。
- 根据真实用户故障和性能数据决定功能优先级，而非继续扩大设置面板。

## 12. 建议立刻创建的任务清单

- [ ] CI：修复 `builds-args`/`matrix.args` 字段错误（ci/pr-ci/release 三处一起查）
- [ ] CI：将 `cargo fmt -p RapidRAW` 改为有效 package 或直接 `cargo fmt --all -- --check`
- [ ] CI：加入 `npm run typecheck`，移除三个 `continue-on-error`
- [ ] Frontend：清零 114 个 TS 错误和 830 个 ESLint errors
- [ ] i18n：补齐/回退约 469 个翻译条目和复数形式
- [ ] Security：环境化 Clerk key，Release 禁止 `pk_test_`
- [ ] Security：配置 CSP，删除不必要的 `shell:default`/`process:default`
- [ ] Security：补 `clip_tokenizer.json` SHA-256
- [ ] Data safety：删除回收站失败后的永久删除回退
- [ ] Dependencies：处理 Clerk/js-cookie high advisory
- [ ] Tooling：npm/pnpm 二选一，冻结安装；固定 Node 版本
- [ ] Versioning：统一 Tauri 1.5.7、Cargo 0.0.0、包/元数据版本源
- [ ] Tests：先补 sidecar、移动/删除、导出命名、调整参数、撤销重做测试
- [ ] Performance：对 AI/全景/HDR/拼图/波形/编辑器做懒加载
- [ ] Performance：建立 24MP/45MP/100MP 基准图集和 P50/P95 指标
- [ ] Architecture：建立 Rust DTO → TypeScript 类型生成与统一任务事件协议
- [ ] UX：统一长任务中心、失败清单、取消/重试和重启恢复
- [ ] Accessibility：建立 Dialog/菜单/滑杆/Canvas 键盘与 axe 测试

## 13. 审计边界与后续复核

本次结论基于代码和本机校验，尚未完成以下需要专项环境的验证：

- Windows、Linux、Android 的真实安装、签名和升级路径。
- 不同厂商 GPU/驱动下的画面一致性与设备丢失恢复。
- 大规模真实 RAW 相机样本、损坏文件和超大图库压力测试。
- Clerk/云 AI/自托管 Connector 的服务端权限、计费和隐私审计。
- 完整手工 UX、色彩准确性和无障碍测试。

建议 P0 修复完成后，在干净分支重新执行本报告第 2 节全部命令，并对三平台 release candidate 做一次真实照片工作流验收，再决定下一版发布。
